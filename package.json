{
  "name": "deadlineos-app",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "pnpm --filter @prioryxai/web dev",
    "build": "pnpm --filter @prioryxai/web build",
    "lint": "pnpm --filter @prioryxai/web lint",
    "test": "pnpm --filter @prioryxai/web test",
    "mobile:sync": "cap sync",
    "mobile:copy": "cap copy",
    "mobile:android": "cap open android"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
